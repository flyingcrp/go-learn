{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug API (Standard)",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}/app",
            "env": {
                "GIN_MODE": "debug",
                "PORT": "8080"
            },
            "args": []
        }
    ]
}